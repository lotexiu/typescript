# API changes

Tags: `major`, `feat`, `fix`, `docs`, `refact`

## Added

- Adicionado `ClassUtils` _(class)_ — src/natives/class/utils.ts

## Removed

- Removido `clamp` _(const)_ — src/natives/math/implementations.ts
- Removido `convert` _(const)_ — src/time/implementations.ts
- Removido `isValidCNPJ` _(const)_ — src/natives/validation/implementations.ts
- Removido `isValidCPF` _(const)_ — src/natives/validation/implementations.ts
- Removido `parse` _(const)_ — src/natives/number/implementations.ts
- Removido `timeExecution` _(const)_ — src/spy/implementations.ts
- Removido `TUFunction` _(type)_ — src/natives/function/implementations.ts

## Changed

- Corrigido `TUString` _(type)_ — src/natives/string/utils.ts
