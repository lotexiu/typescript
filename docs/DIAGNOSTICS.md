# Diagnósticos

_Gerado por `scripts/doc/generate.ts`._

0 erro(s), 35 aviso(s).

## Erros

_Nenhum._

## Avisos

- `src/aho-corasick/model.ts:12` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/aho-corasick/model.ts:13` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/aho-corasick/model.ts:14` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/aho-corasick/model.ts:15` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/aho-corasick/model.ts:16` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/aho-corasick/model.ts:17` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/aho-corasick/model.ts:26` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/aho-corasick/model.ts:46` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/aho-corasick/model.ts:68` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/cache/model.ts:4` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/cache/model.ts:5` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/cache/model.ts:6` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/cache/model.ts:7` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/language/node/model.ts:15` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/language/node/model.ts:67` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/language/node/model.ts:76` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:9` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:12` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:19` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:31` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:43` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:44` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:45` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:47` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:49` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:54` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:96` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:104` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:110` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/model.ts:120` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/node/model.ts:25` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/node/model.ts:29` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/node/model.ts:45` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/parser/node/model.ts:48` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
- `src/signal/node.ts:69` **prefer-hash-private** — `private` do TypeScript — prefira `#nome` (privado real de runtime)
