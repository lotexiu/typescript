{
  "name": "@lotexiu/typescript-main",
  "version": "1.0.0",
  "private": "false",
  "scripts": {
    "build": "pnpm clean && vite build",
    "dev": "vite build --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@lotexiu/typescript-config": "workspace:*"
  },
  "dependencies": {
    "@lotexiu/vite-utils": "workspace:*",
    "colorjs.io": "^0.5.2"
  }
}
