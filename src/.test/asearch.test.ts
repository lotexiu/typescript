import { describe, expect, it, vi } from 'vitest';
import { ASearch } from '..';

describe('Tests with ASearch', () => {
	it('equals', () => {
		const search = new ASearch({
			name: '0(1/1/5)2',
		});
	})
});
