/** Constructor/`setLimits` options for `NumberPlugin` — `min`/`max` clamp the parsed value, they don't validate it. */
interface TNumberPluginOptions {
	min?: number;
	max?: number;
}

export type {
	TNumberPluginOptions
}
