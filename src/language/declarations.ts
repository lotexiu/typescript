/**
 * @internal
 * Prefixo do `kind` de um token-placeholder de escopo aninhado (`scope:block`, `scope:paren`).
 * `G.into` / `G.placeholder` casam por esse `kind`.
 */
const SCOPE_KIND_PREFIX = "scope:"

export {
	SCOPE_KIND_PREFIX,
}
